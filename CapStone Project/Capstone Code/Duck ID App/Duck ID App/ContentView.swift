import SwiftUI

// Root SwiftUI view for the app.
// Keep this file small: the real screens live in the feature files next to it.
struct ContentView: View {
    var body: some View {
        MainTabView()
    }
}

#Preview {
    ContentView()
}
